import React, { Component } from 'react'

export class PortfolioItem extends Component {
  render() {
    const {id, name, content, info} = this.props; 
    return (
      <div>PortfolioItem</div>
    )
  }
}

export default PortfolioItem